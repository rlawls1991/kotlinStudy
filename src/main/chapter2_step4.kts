/**
 * 경고를 현명하게 해준다.
 * 실행
 * Warring 발견
 * kotlinc-jvm -script chapter2_step4.kts
 * Warring을 오류로 인지
 * kotlinc-jvm -Werror -script chapter2_step4.kts
 */
fun compute(n: Int) = 0
println(compute(4))
